services:
  - type: worker
    name: telegram-link-bot
    env: python
    buildCommand: ""
    startCommand: "python bot.py"
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
